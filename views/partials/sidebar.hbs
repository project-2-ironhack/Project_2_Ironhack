    <!-- Sidebar -->
    <ul class="navbar-nav bg-gradient-primary sidebar sidebar-dark accordion" id="accordionSidebar">

      <!-- Sidebar - Brand -->
      <a class="sidebar-brand d-flex align-items-center justify-content-center" href="index.html">
        <div class="sidebar-brand-icon rotate-n-15">
          <i class="fab fa-app-store-ios"></i>
        </div>
      </a>

      <!-- Divider -->
      <hr class="sidebar-divider my-0">

      <!-- Nav Item - Dashboard -->
      <li class="nav-item active">
        <a class="nav-link " href="/set">
          <i class="fas fa-search fa-search-alt"></i>
          <span>New Search</span></a>
      </li>

      <!-- Divider -->
      <hr class="sidebar-divider">

      {{#if dashboard}} 
        <!-- Heading -->
        <div class="sidebar-heading">
          Establecimientos
        </div>
        <form id="sidebarForm" action="/set/{{query._id}}" method="POST">
          <div class="overflow">
            {{#each placesObj as |place|}}
              <!-- Nav Item - Charts -->
              <li class="nav-item">
                <span class="nav-link py-1">
                  <input name="establecimiento" class="" type="checkbox" value="{{place.bbva}}"
                    {{#hasQueryCategory ../query place.bbva }}checked="checked"{{/hasQueryCategory}}> 
                  <label for="graph-{{place}}" class="form-check-label p-0 col-8">{{place.charter}}</label>
                  <i class="fas fa-fw {{place.fa}} "></i>
                </span>
              </li>
            {{/each}}
          </div>
          <div class="text-center col-12 mb-2">
            <button type="submit" class="btn btn-primary mr-3">Submit</button>
          </div>
        </form>
      {{/if}}

      <!-- Sidebar Toggler (Sidebar) -->
      <div class="text-center d-none d-md-inline">
        <button class="rounded-circle border-0" id="sidebarToggle"></button>
      </div>

    </ul>